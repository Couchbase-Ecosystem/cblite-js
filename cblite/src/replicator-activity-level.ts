export enum ReplicatorActivityLevel {
  // eslint-disable-next-line
  STOPPED = 0,
  // eslint-disable-next-line
  OFFLINE = 1,
  // eslint-disable-next-line
  CONNECTING = 2,
  // eslint-disable-next-line
  IDLE = 3,
  // eslint-disable-next-line
  BUSY = 4,
}
